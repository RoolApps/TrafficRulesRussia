<Page
    x:Class="Tickets.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tickets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>
        
        <Border BorderBrush="WhiteSmoke" Grid.Row="0" BorderThickness="3" Margin="10">
            <TextBlock x:Name="txtNewExam" Text="Новый экзамен" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="50" Tapped="txtNewExam_Tapped" />
        </Border>
        <Border BorderBrush="WhiteSmoke" Grid.Row="1" BorderThickness="3" Margin="10">
            <TextBlock x:Name="txtStatistics" Text="Статистика" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="50" />
        </Border>
    </Grid>
</Page>
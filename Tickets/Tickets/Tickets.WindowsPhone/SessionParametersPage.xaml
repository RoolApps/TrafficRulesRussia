<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tickets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:SQLiteShared.Models"
    x:Class="Tickets.SessionParametersPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <CheckBox x:Name="chkRandomTicket" Content="Случайный билет" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Checked="chkRandomTicket_Checked" Unchecked="chkRandomTicket_Unchecked"/>
        <Button x:Name="btnStart" Content="Начать" HorizontalAlignment="Left" Margin="150,567,0,0" VerticalAlignment="Top" Click="btnStart_Click"/>
        <ListView x:Name="listTickets" HorizontalAlignment="Left" Height="475" Margin="10,74,0,0" VerticalAlignment="Top" Width="344" SelectionMode="Multiple">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Num}" FontSize="20"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

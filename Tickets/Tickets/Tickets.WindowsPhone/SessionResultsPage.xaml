<Page
    x:Class="Tickets.SessionResultsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tickets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Pivot x:Name="pivot" Grid.Row="0">
            <Pivot.ItemTemplate>
                <DataTemplate>
                    <PivotItem Header="{Binding Path=Num}">

                        <SemanticZoom DoubleTapped="SemanticZoom_DoubleTapped">
                            <SemanticZoom.ZoomedInView>
                                <ListView ItemsSource="{Binding Questions}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <TextBlock FontSize="20" Text="{Binding Num}"/>
                                                <ListView ItemsSource="{Binding Answers}">
                                                    <ListView.ItemTemplate>
                                                        <DataTemplate>
                                                            <TextBlock FontSize="12" Text="{Binding Text}" />
                                                        </DataTemplate>
                                                    </ListView.ItemTemplate>
                                                </ListView>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </SemanticZoom.ZoomedInView>
                            <SemanticZoom.ZoomedOutView>
                                <GridView ItemsSource="{Binding Questions}">
                                    <GridView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapGrid ItemHeight="80" ItemWidth="80" HorizontalAlignment="Center" MaximumRowsOrColumns="5">

                                            </WrapGrid>
                                        </ItemsPanelTemplate>
                                    </GridView.ItemsPanel>
                                    <GridView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="70" Height="70" BorderBrush="White" BorderThickness="2">
                                                <TextBlock Text="{Binding Num}" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </GridView.ItemTemplate>
                                </GridView>
                            </SemanticZoom.ZoomedOutView>
                        </SemanticZoom>
                    </PivotItem>
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>
        <Button Grid.Row="1" Content="Главное меню" x:Name="btnMainMenu" HorizontalAlignment="Center"/>
    </Grid>
</Page>

<Page
    x:Class="Tickets.SessionResultsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tickets"
    xmlns:utils="using:Utils"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <utils:SemanticZoomVisibilityConverter x:Key="VisibilityConverter" />
        <utils:CollectionViewSourceConverter x:Key="SourceConverter" />
        <local:BooleanHolder x:Key="IsPivotVisible" x:Name="IsPivotVisible" />
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Pivot x:Name="pivot" Grid.Row="0">
            <Pivot.ItemTemplate>
                <DataTemplate>
                    <PivotItem Header="{Binding}">
                        <SemanticZoom x:Name="semanticZoom" DataContext="{Binding Path=Questions, ConverterParameter=Answers, Converter={StaticResource SourceConverter}}" DataContextChanged="semanticZoom_DataContextChanged" ViewChangeStarted="semanticZoom_ViewChangeStarted">
                            <SemanticZoom.ZoomedInView>
                                <ListView Visibility="{Binding ElementName=semanticZoom, Path=IsZoomedInViewActive, Converter={StaticResource VisibilityConverter}}" ItemsSource="{Binding}">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock FontSize="12" Text="{Binding Text}" />
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.GroupStyle>
                                        <GroupStyle>
                                            <GroupStyle.HeaderTemplate>
                                                <DataTemplate>
                                                    <TextBlock FontSize="20" Text="{Binding Number}"/>
                                                </DataTemplate>
                                            </GroupStyle.HeaderTemplate>
                                        </GroupStyle>
                                    </ListView.GroupStyle>
                                </ListView>
                            </SemanticZoom.ZoomedInView>
                            <SemanticZoom.ZoomedOutView>
                                <GridView ScrollViewer.IsHorizontalScrollChainingEnabled="False" Margin="0,150,0,0" >
                                    <GridView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapGrid ItemHeight="80" ItemWidth="80" HorizontalAlignment="Center" MaximumRowsOrColumns="5" />
                                        </ItemsPanelTemplate>
                                    </GridView.ItemsPanel>
                                    <GridView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="70" Height="70" BorderBrush="White" BorderThickness="2">
                                                <TextBlock Text="{Binding Group.Number}" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </GridView.ItemTemplate>
                                </GridView>
                            </SemanticZoom.ZoomedOutView>
                        </SemanticZoom>
                    </PivotItem>
                </DataTemplate>
            </Pivot.ItemTemplate>
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Number}" Visibility="{Binding Source={StaticResource IsPivotVisible}, Path=Value, Converter={StaticResource VisibilityConverter}}" FontSize="20"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
        </Pivot>
        <Button Grid.Row="1" Content="Главное меню" x:Name="btnMainMenu" HorizontalAlignment="Center"/>
    </Grid>
</Page>

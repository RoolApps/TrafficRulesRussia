<Page xmlns:XAMLMarkup="using:XAMLMarkup" 
    x:Class="Tickets.QuestionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tickets"
    xmlns:utils="using:Utils"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Resources>
            <utils:ImageConverter x:Key="ImageConverter"></utils:ImageConverter>
            <local:BorderColorConverter x:Key="BorderColorConverter"></local:BorderColorConverter>
        </Grid.Resources>
        <XAMLMarkup:FlippingCanvas x:Name="flippingCanvas" MotionDuration="500">
            <XAMLMarkup:FlippingCanvas.CanvasContent>
                <XAMLMarkup:PagedCanvas>
                    <XAMLMarkup:PagedCanvas.CanvasContent>
                        <Canvas HorizontalAlignment="Left">
                            <TextBlock Text="{Binding Path=Text}" HorizontalAlignment="Center" FontSize="20" Margin="0,10,0,499" VerticalAlignment="Center" Height="131" Width="380" TextWrapping="WrapWholeWords" TextAlignment="Center"/>
                            <Image Source="{Binding Path=Image, Converter={StaticResource ImageConverter}}" Height="142.5" Width="380" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,146,0,0"/>
                            <ListView ItemsSource="{Binding Path=Answers}" HorizontalAlignment="Left" Height="323" Margin="10,307,0,0" VerticalAlignment="Top" Width="380">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{Binding Path=IsSelected, Mode=OneWay, Converter={StaticResource BorderColorConverter}, UpdateSourceTrigger=PropertyChanged}" BorderThickness="2" Margin="0,0,0,10">
                                            <TextBlock Text="{Binding Path=Text}" FontSize="20" Width="380" TextWrapping="WrapWholeWords" Tapped="TextBlock_Tapped"/>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Canvas>
                    </XAMLMarkup:PagedCanvas.CanvasContent>
                </XAMLMarkup:PagedCanvas>
            </XAMLMarkup:FlippingCanvas.CanvasContent>
        </XAMLMarkup:FlippingCanvas>
    </Grid>
</Page>
